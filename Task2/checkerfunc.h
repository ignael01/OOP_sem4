#pragma once

#include <vector>

bool checkVectRandom(std::vector<int> test_vect);
bool checkVectFactorial(std::vector<int> test_vect);
bool checkArrRandom(int* test_arr, size_t size);
bool checkArrFactorial(int* test_arr, size_t size);